apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway-bff.fullname" . }}
  labels:
    {{- include "api-gateway-bff.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "api-gateway-bff.selectorLabels" . | nindent 4 }}

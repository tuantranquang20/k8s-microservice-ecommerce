# user-service environment variables
# Copy to .env for local development — never commit .env!

# Server
PORT=3000
NODE_ENV=development

# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=users
DB_USER=postgres
DB_PASSWORD=changeme

# JWT signing secret (use Vault in production for rotation)
JWT_SECRET=super-secret-change-in-prod
JWT_EXPIRES_IN=7d

# Service-to-service base URL (used for health checks from other services)
SERVICE_NAME=user-service

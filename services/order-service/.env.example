# order-service environment variables
PORT=8080
GIN_MODE=debug

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=orders
DB_USER=postgres
DB_PASSWORD=changeme

# Redis (for async order.created publication)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# JWT (same secret as user-service)
JWT_SECRET=super-secret-change-in-prod

SERVICE_NAME=order-service
